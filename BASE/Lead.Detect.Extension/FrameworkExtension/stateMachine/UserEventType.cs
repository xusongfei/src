namespace Lead.Detect.FrameworkExtension.stateMachine
{
    public enum UserEventType
    {
        START,
        STOP,
        RESET,
        PAUSE,
        CONTINUE,

        ALARM,

        LOG,
    }
}